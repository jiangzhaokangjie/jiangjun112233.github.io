---
name: xiaolan-ip-illustrations
description: 生成 姜老师 风格的中文正文配图。用于用户要求为中文文章、帖子、博客、Notion 文档、工作流文档、方法论、流程、结构、状态、隐喻或观点生成"游泳""训练""示范""教学""正文配图""文章插图""配图建议""shot list""去标题/改图"等任务；默认使用姜老师 IP、纯白手绘教学风、少量红橙蓝批注、简洁清爽且专业的视觉风格。
---

# 姜老师游泳示范正文配图

## 核心定位

为中文文章（尤其是运动训练、技能教学、成长方法论、专注力/习惯/流程类内容）设计和生成 16:9 横版正文配图。目标不是做商业插画、PPT 信息图或可爱卡通，而是把文章里的关键判断、流程、结构、状态或隐喻，变成一张清爽、专业、可读但不说明书的手绘教学解释图。

默认视觉 IP 是"**姜老师**"：一位帅气、专业的男性游泳教练、游泳专项训练示范模特（识别点：白帽印字/黑镜/深裤）。也可切换为女性 IP"**丽丽**"（黄帽/黑镜/浅蓝衣，见 xiaolan-ip.md 第八章）。无论启用哪个 IP，它必须参与画面的核心动作——用标准游泳技术动作或训练示范来诠释内容，不能只是站在旁边当装饰。

## 先读这些参考

按任务需要读取，不要一次塞满上下文：

- `references/style-dna.md`：风格 DNA、颜色、文字、禁忌。
- `references/xiaolan-ip.md`：姜老师/丽丽 IP 的形象、性格、动作库和禁忌。
- `references/composition-patterns.md`：结构类型、原创隐喻方法和反复刻规则。
- `references/prompt-template.md`：单张生图提示词模板。
- `references/qa-checklist.md`：生成后检查和迭代规则。
- `assets/examples/`：只作低频视觉校准，不进入默认生成路径。不要照抄这些案例的构图、物件或标注。

## 工作流

### 1. 消化正文

先读用户给的正文、链接、Notion 页面、Markdown 文件或截图内容。提炼：

- 核心观点是什么
- 哪些段落承担认知转折
- 哪些内容适合用图解释
- 哪些地方只适合文字，不需要图

不要平均配图。优先选择"认知锚点"，例如：核心判断、两个断点、输入输出闭环、分流、前后对比、承接路径、常见坑、角色状态变化。

把抽象概念尽量"翻译"成游泳训练语境：专注力 → 流线型滑行；刻意练习 → 分解动作重复；反馈闭环 → 教练纠正 + 看表计时；坚持 → 打腿耐力。

### 2. 先出配图策略

如果用户只是说"分析怎么配图 / 思考哪些地方需要配图"，先给 shot list。每张图写清楚：

- 放在哪个段落后
- 图的主题
- 核心意思
- 结构类型
- 姜老师/丽丽在图里做什么（用哪个游泳技术/训练动作来隐喻）
- 建议元素
- 建议中文标注词

默认 4-8 张。文章很短时 1-3 张；长文也不要轻易超过 9 张。够用就好，避免把正文做成画册。

### 3. 单张生成

如果用户明确要求"生成 / 输出 / 做图 / 帮我生成"，不要停下来等确认；用内置 `image_gen` 每张单独生成。不要把多张图拼在一张里。

**IP 选择**：默认生成男性姜老师。若用户明确要求"女性 IP / 用丽丽 / 换成女生"，生图 prompt 的 [ROLE] 段整体改用丽丽的英文稳定描述（见 xiaolan-ip.md 第八章），其余动作 / 场景 / 禁字规则不变。

每张图只讲一个核心结构。**生图时画面不得出现任何文字、标签或批注**——中文文字由后续叠字脚本确定性地叠加，不在生成阶段让模型画字（扩散模型画中文天生不可靠，必出假字/错字）。提示词必须包含：

- 16:9 横版中文正文配图
- 纯白背景
- 黑色手绘线稿（姜老师/丽丽为带细节的写实手绘人物，非黑色块状剪影）
- **NO text, NO labels, NO handwritten annotations, NO Chinese characters in the image**
- 大量留白
- 姜老师/丽丽作为核心动作主体（正在做标准游泳技术示范或训练动作）
- 禁止 PPT、商业插画、幼稚可爱、复杂架构、左上角类型标题

不要复刻过往案例。案例只提供风格密度和姜老师/丽丽参与方式，不能直接复用某张已有构图，除非用户明确要求复刻。每次都要从当前文章重新发明一个"用游泳训练示范来诠释概念"的奇怪但成立的隐喻。

### 3.5 叠字（生图后）

每张无字画面生成后，立即调用 `scripts/overlay_annotations.py` 把中文批注叠加到图上。

用法：
```bash
python scripts/overlay_annotations.py \
    --image <生成的无字图.png> \
    --out   <最终输出.png> \
    --annotations <annotations.json>
```

`annotations.json` 每项包含：
```json
{
  "text": "一条直线",       // 批注文字（必填）
  "color": "red",          // red | orange | blue
  "x": 0.30,               // 文字中心横坐标（归一化 0~1）
  "y": 0.40,               // 文字中心纵坐标（归一化 0~1）
  "size": 54,              // 字号（基于 1920 宽参考）
  "rotate": -5,            // 可选旋转角度（模拟手写抖动）
  "circle": true,          // 可选圈注
  "underline": true,       // 可选下划线
  "arrow_from": [0.20,0.55] // 可选箭头起点
}
```

字体默认使用系统楷体 `C:\Windows\Fonts\simkai.ttf`（手写风），颜色严格遵循 style-dna 配色纪律（红 #E5484D / 橙 #F59E0B / 蓝 #2563EB）。一张图最多 2-4 个批注点，宁少勿多。

### 4. 检查与迭代

生成后检查 `references/qa-checklist.md`。如果出现以下问题，优先重生成或局部编辑：

- 姜老师只是装饰，没有承担核心动作
- 姜老师没有白色印字泳帽 / 黑色泳镜 / 深色泳裤（识别点缺失）
- 画面太满
- 太像流程图/PPT
- **生图阶段画面意外出现了文字/标签（必须重生成无字版）**
- 叠字后批注文字有误（重新修正 annotations.json 并重跑脚本）
- 左上角出现"常见坑/流程图/系统架构图"等标题
- 画风太可爱、幼稚、死板
- 背景不是干净白底
- 姜老师被画成女性、儿童或卡通形象

### 5. 保存交付

如果用户在 workspace 内工作，把最终图复制到：

```text
assets/<article-slug>-illustrations/
```

按顺序命名：

```text
01-topic-name.png
02-topic-name.png
```

保留原始生成文件，不要覆盖已有资产，除非用户明确要求替换。

## 输出口径

生成前的策略输出要短而准。生成后的交付要包含：

- 生成了几张
- 每张图的用途
- 保存路径
- 哪些图最稳，哪些图是可选

不要长篇解释风格理论；让图自己说话。
